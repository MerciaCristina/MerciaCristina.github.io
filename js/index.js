function clock(){currentDate=new Date,hour=currentDate.getHours(),min=currentDate.getMinutes(),sec=currentDate.getSeconds(),formatedTime=hour+":"+min+":"+String(sec).padStart(2,"0"),document.querySelector(".time").textContent=formatedTime}clock(),setInterval(clock,1e3);